<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:paddingVertical="8dp"
    android:paddingHorizontal="16dp">

    <androidx.appcompat.widget.AppCompatImageView
        android:id="@+id/travel_image_view"
        android:layout_width="100dp"
        android:layout_height="100dp" />

    <TextView
        android:id="@+id/name_text_view"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignTop="@id/travel_image_view"
        android:layout_marginHorizontal="8dp"
        android:layout_toStartOf="@id/arrow_text_view"
        android:layout_toEndOf="@id/travel_image_view"
        android:textStyle="bold" />

    <TextView
        android:id="@+id/introduction_text_view"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/name_text_view"
        android:layout_alignStart="@id/name_text_view"
        android:layout_alignEnd="@id/name_text_view"
        android:layout_marginTop="5dp"
        android:ellipsize="end"
        android:maxLines="3" />

    <TextView
        android:id="@+id/arrow_text_view"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentEnd="true"
        android:layout_centerInParent="true"
        android:text="＞" />

</RelativeLayout>